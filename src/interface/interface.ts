export interface Message {
  id: string;
  name: string;
  text: string;
}

export interface Payload {
  id: string;
  name: string;
  text: string;
}

export interface User {
  id: string;
  name: string;
}
